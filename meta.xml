<dracon-dev>
	<!-- SCRIPT's INFO's -->
	<info author = 'dracoN*' type = 'script' version = '1.0.0'/>
	<!-- SCRIPT's CONFIG's -->
	<script src = 'config/index.lua' type = 'shared' cache = 'false'/>
	<script src = 'config/itens.lua' type = 'shared' cache = 'false'/>
	<!-- SCRIPT's SRC's -->
	<script src = 'src/utils.lua' type = 'shared' cache = 'false'/>

	<script src = 'src/models/index.lua' type = 'shared' cache = 'false'/>
	<script src = 'src/models/request.lua' type = 'shared' cache = 'false'/>
	<!-- SCRIPT's MODULE's -->
	<script src = 'src/modules/server/**/*.lua'/>
	<script src = 'src/modules/client/**/*.lua' type = 'client' cache = 'false'/>
	<!-- ASSET's FONT's -->
	<file src = 'assets/fonts/*.ttf'/>
	<!-- ASSET's IMAGE's -->
	<file src = 'assets/images/*.png'/>
	<!-- ASSET's IMAGE's (ICON's) -->
	<file src = 'assets/images/icons/*.png'/>
	<!-- VERSION's RESOURCE's -->
	<min_mta_version server = '1.6.0' client = '1.6.0'/>
	<!-- EXPORT's RESOURCE's -->
	<export function = 'getItems' type = 'shared'/>
	<export function = 'getConfig' type = 'shared'/>

	<export function = 'getConnection'/>
</dracon-dev>